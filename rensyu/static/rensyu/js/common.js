let Util = {};
Util.common = {
  /**
   * 확인용 dialog
   * 
   * @param {String} msg 확인용 메시지
   * @returns bool
   */
  confirmDialog : function(msg) {
    // alert('confirm' + msg);
    return window.confirm(msg);
  },

  // 경고용 다이얼로그
  alertDialog : function(msg) {

  }
};

Util.validate = {

}

